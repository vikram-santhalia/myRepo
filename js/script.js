/*function animate_bar() {
  var bar = $('.progress-bar');
  $(function(){
    $(bar).each(function(){
      var bar_width = $(this).attr('aria-valuenow');
      var bar_max = $(this).attr('aria-valuemax');
      var per = (bar_width/bar_max) * 100;
      $(this).width(per + '%');
    });
  });
}

setTimeout(function (){
  animate_bar();
}, 200); */

/*$( document ).ready(function() {
  $('input[type=file]').bootstrapFileInput();
  $('.file-inputs').bootstrapFileInput();
});
*/